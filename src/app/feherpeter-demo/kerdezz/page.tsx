import type { Metadata } from "next";
import Kerdezz from "../../_demo/feherpeter/screens/Kerdezz";

export const metadata: Metadata = { title: "Kérdezz Pétertől" };

export default function Page() {
  return <Kerdezz />;
}
